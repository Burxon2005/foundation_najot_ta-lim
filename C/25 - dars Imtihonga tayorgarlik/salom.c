#include<stdio.h>
#include<string.h>
#include<stdlib.h>


int main(){

    /*
    char user_str[10];

    scanf("%s",user_str);
    char copy[strlen(user_str)];
    //user_str = (char*)malloc(strlen(user_str)*sizeof(char));
    //copy = (char*)malloc(strlen(copy)*sizeof(char));

    for (int i = 0; i < strlen(user_str)/2; i++){
        copy[i] = user_str[i];
    }

    for (int i = strlen(user_str)/2; i < strlen(user_str); i++){
        user_str[a] = user_str[i];
        a++;
    }
    strcpy(user_str+strlen(user_str)/2,"");
    strcat(user_str,copy);

    printf("%s ",user_str);
    
    
    
    if (strlen(user_str)%2==0){
        strncat(user_str,user_str,strlen(user_str)/2);
        printf("%s\n",user_str);
        user_str[strlen(user_str)-1] == '\0';
    }else{
        strncat(user_str,user_str,(strlen(user_str)/2)+1);
        printf("%s\n",user_str);
    }
    strcpy(user_str,user_str+(strlen(user_str)/2)-2);
    printf("%s\n",user_str);
    */

    int 


    return 0;
}