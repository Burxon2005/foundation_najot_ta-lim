#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main() {
    /*
    int a[10];
    srand(time(0));
    for (int i = 0; i < 10; ++i) {
        a[i] = 1+ rand()%100;
        printf("%d ", a[i]);
    }
    printf("\n");
    for (int i = 0; i < 10; ++i) {
        if (a[i] % 2 == 0) {
            printf("%d ", a[i]);
        }
    }
    printf("\n");
    for (int i = 9; i >= 0; --i) {
        if (a[i] % 2 != 0) {
            printf("%d ", a[i]);
        }
    }
     */


    // basketbol misol






    return 0;
}